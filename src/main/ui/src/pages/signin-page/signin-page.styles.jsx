import { createStyles } from "@material-ui/core/styles";

const styles = (theme) =>
  createStyles({
    signin: {
      paddingTop: theme.spacing(8),
      margin: "0 auto",
      maxWidth: 450,
    },
  });

export default styles;
